{{define "content"}}
<div class="container">
  <div class="row">
    <div class="jumbotron">
      <h1>Hello, World!</h1>
      <p>Welcome to your new Go web project.</p>
      {{range .}}
          <li><a href="{{.ID}}">{{.Created}} {{.ID}}</a></li>
      {{end}}
      <p></p>
    </div>
  </div>
</div>
{{end}}
